// NutriAI Prisma schema.
// Models are added feature-by-feature. Scaffold intentionally ships empty.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
