# NutriAI frontend env
# Copy this to .env.local and adjust for your environment.

# Base URL of the NutriAI backend (Express API).
# In production this is your deployed API host.
NEXT_PUBLIC_API_URL=http://localhost:4000
